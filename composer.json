{
    "name": "be-framework/skeleton",
    "type": "project",
    "autoload": {
        "psr-4": {
            "Be\\Skeleton\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Be\\Skeleton\\": "tests/"
        }
    },
    "require": {
        "be-framework/be": "0.x-dev",
        "koriym/semantic-logger": "1.x-dev#af49a74d1a764f69f17a88a168a868e5e0738648 as 0.5.x-dev",
        "ray/di": "^2.18"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "scripts": {
        "dev": "php bin/be.php 'hello?name=World'",
        "profile": "XDEBUG_MODE=trace php -d xdebug.use_compression=0 -d extension=xhprof.so bin/be.php 'hello?name=World'",
        "stree": "vendor/bin/stree $(ls -t var/log/*.json | head -1)",
        "stree:full": "vendor/bin/stree --full $(ls -t var/log/*.json | head -1)"
    },
    "scripts-descriptions": {
        "dev": "Run the Hello World pipeline in dev mode — writes a semantic log to var/log/<timestamp>.json without Xdebug/XHProf profiling.",
        "profile": "Run the Hello World pipeline with semantic profiling — writes a semantic log to var/log/<timestamp>.json (uncompressed Xdebug trace + xhprof enabled).",
        "stree": "Render the latest var/log/*.json as a compact tree.",
        "stree:full": "Render the latest var/log/*.json as a semantic full tree with full props and close.profile details."
    }
}
