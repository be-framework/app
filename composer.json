{
    "name": "be-framework/skeleton",
    "type": "project",
    "autoload": {
        "psr-4": {
            "Be\\Skeleton\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Be\\Skeleton\\": "tests/"
        }
    },
    "require": {
        "be-framework/be": "0.x-dev",
        "koriym/semantic-logger": "^0.4",
        "ray/di": "^2.18"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "scripts": {
        "app": "MODULE=app php bin/app.php 'hello?name=World'",
        "dev": "XDEBUG_MODE=trace php -d xdebug.use_compression=0 -d extension=xhprof.so bin/app.php 'hello?name=World'",
        "stree": [
            "@dev",
            "vendor/bin/stree $(ls -t var/log/*.json | head -1)"
        ],
        "stree:full": [
            "@dev",
            "vendor/bin/stree --full $(ls -t var/log/*.json | head -1)"
        ]
    }
}
